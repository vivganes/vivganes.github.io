<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title><![CDATA[Category: java | Vivek Ganesan]]></title>
  <link href="http://vivekganesan.com/blog/categories/java/atom.xml" rel="self"/>
  <link href="http://vivekganesan.com/"/>
  <updated>2015-10-24T11:53:43+05:30</updated>
  <id>http://vivekganesan.com/</id>
  <author>
    <name><![CDATA[Vivek Ganesan]]></name>
    <email><![CDATA[vivek@vivekganesan.com]]></email>
  </author>
  <generator uri="http://octopress.org/">Octopress</generator>

  
  <entry>
    <title type="html"><![CDATA[Design and Code to Choose the Husband Using MapReduce]]></title>
    <link href="http://vivekganesan.com/blog/2014/03/24/design-and-code-to-choose-the-husband-using-mapreduce/"/>
    <updated>2014-03-24T10:37:57+05:30</updated>
    <id>http://vivekganesan.com/blog/2014/03/24/design-and-code-to-choose-the-husband-using-mapreduce</id>
    <content type="html"><![CDATA[<p>In the last blog post &ldquo;<a href="http://vivekganesan.com/blog/2014/03/22/what-is-mapreduce-an-exercise-using-activity-based-learning/">What is MapReduce? &ndash; An Exercise using Activity Based Learning</a>&rdquo;, the methodology of MapReduce was explained.  In this blog post, let me take it a step further.  Let us do the technical design and Java coding for the same problem &ldquo;Choosing the Husband&rdquo; explained in the previous blog post.</p>

<h3>Recap</h3>

<p>Remember that there are three phases in the MapReduce methodology.<br/>
1. Map<br/>
2. Shuffle &amp; Sort (Which I simply call as &lsquo;Grouping&rsquo;)<br/>
3. Reduce</p>

<p>Here is a recap of characters in our story.</p>

<p><img class="center" src="/images/character_names.png"></p>

<p>Before getting on to explain the technical design of the solution, let me highlight some important points in the MapReduce methodology.</p>

<ol>
<li>The input is split into multiple input-splits before being passed to Mappers.</li>
<li>Each mapper worker is assigned one input-split.</li>
<li>The Map phase has input format of key,value and output format of key,value.</li>
<li>The Shuffle &amp; Sort phase has input format of key,value and output format of key,{list of values}</li>
<li>The Reduce phase has input format of key,{list of values} and output format of key,value</li>
</ol>


<h3>Assumption</h3>

<p>&ldquo;Choosing the Husband&rdquo; problem explained in the previous blog post has images.  We had to identify people from images.  So, we should be writing code for image processing in this blog post, right?  No!  We are going to get rid of that image processing complexity by a convenient assumption.</p>

<p>The assumption is this: We will have two input files, with eight lines each.  Each line would have two words.  Each of these words denote the name of the person identified in the image.</p>

<p>For example, if the first photo contained Jaja and Jojo, the first line of the first file would read the following.</p>

<p><code>text
jaja jojo
</code>
Second line denotes the people present in second photo and so on upto the first eight photos.  Thereafter, entries go into the second input file.  Thus, we would end up with two files with eight lines each, each line having exactly two words.</p>

<h3>Key-Value Input?? How?</h3>

<p>If you have not noticed, see that I have mentioned in the &ldquo;Recap&rdquo; section that Map phase has input format of key,value.</p>

<p>Map is the first phase of the MapReduce process.  So, these two files, which we have, with two words in each line, should be the input to the Map phase.  Is this a key,value pair?  Not at all!</p>

<p>MapReduce framework of Hadoop does this for you by doing the following default assignments for each line in the files.<br/>
<strong>Key</strong> &ndash; Character Position of the starting of the line<br/>
<strong>Value</strong> &ndash; The actual content of the line</p>

<p>For example, if the first line of the first file contains <code>jaja jojo</code>, then, for the first line (or split),<br/>
<strong>Key</strong> &ndash; 0<br/>
<strong>Value</strong> &ndash; <code>jaja jojo</code></p>

<p>If the second line contained <code>jiji jojo</code>, then, for that line(or split), <br/>
<strong>Key</strong> &ndash; 9<br/>
<strong>Value</strong> &ndash; <code>jiji jojo</code></p>

<p>The key is nothing but the position of the line&rsquo;s first character in the file, starting from zero.  This key is not at all relevant as far as this &ldquo;Finding the Husband&rdquo; problem is concerned.</p>

<h3>Mapper &ndash; Sample I/O</h3>

<p>Hadoop&rsquo;s MapReduce framework, by default, passes one split each to a mapper process.  By default, one split is equal to one line.</p>

<p>Thus, sample input of Mapper is <br/>
<code>text Input to a Single Mapper
jaja jojo
</code>
According to our logic, we need the following output from this mapper.
<code>text Output of a Single Mapper
jaja - 1
jojo - 1
</code>
If the second line is
<code>text Input to second Mapper
jiji jojo
</code>
Then, we the output of second Mapper would be
<code>text Output of Second Mapper
jiji - 1
jojo - 1
</code></p>

<h3>Shuffle and Sort &ndash; Sample I/O</h3>

<p>If both the above mappers' outputs are piped to the input of the next phase, that is, Shuffle and Sort, the output of Shuffle and Sort phase would be<br/>
<code>text Output of Shuffle &amp; Sort
jaja - {1}
jojo - {1,1}
jiji - {1}
</code></p>

<h3>Reducer &ndash; Sample I/O</h3>

<p>Reducer just adds the number of ones and consolidates the result as a single number.  So, if the above Shuffle and Sort output is piped to reducers, the output of first reducer would be
<code>text Output of first reducer
jaja - 1
</code></p>

<p>The output of the second reducer would be
<code>text Output of second reducer
jojo - 2 (that is, 1+1)
</code></p>

<p>The output of the third reducer would be
<code>text Output of third reducer
jiji - 1
</code></p>

<p>Remember, the above sample values were only &ldquo;samples&rdquo;, chained all the way through the MapReduce process.  These are not the actual values taken from the sixteen photos in the previous post.  If not, how would we end up with only three reductions.  We must be ending up with five reductions, one for each contestant for the post of husband, right?</p>

<p>Hoping that you follow what I have told until now, I am proceeding to the dare-devilry of writing the code.</p>

<h3>A Good News</h3>

<p>There is a good news.  Shuffle and Sort phase is already coded in the Hadoop MapReduce framework.  You have to code for Map and Reduce phases only.  Depending on the lines of code you write in Map and Reduce phases, your MapReduce code could do anything from a simple Word count to some complex Sentimental Analysis.</p>

<h3>Coding the Mapper</h3>

<p>We will be using the language Java to code our program.  Refer to the sample I/O section of the mapper above.  Remember that one line is called one split.  Also, a mapper does processing on a split at a time.</p>

<p>We will be writing a Java class to do the mapping functionality.  This class will be called <code>MatchMakerMapper</code>.  At this juncture, I must tell you that all classes doing the mapping functionality must extend another class called <code>Mapper</code> and must define a method called <code>map()</code>.</p>

<p>Thus, we are going to write a code, which would approximately look like the one below.</p>

<p>```java
pubic class MatchMakerMapper extends Mapper {</p>

<p>  public void map(){</p>

<p>  }</p>

<p>}
```</p>

<p>We will add more details to the above skeleton as we progress through this blog post.</p>

<p>We need to specify the input and output data types of the mapper somewhere right?  Even before that, what options do we have for input and output data types?<br/>
1. Text &ndash; for string data<br/>
2. IntWritable or LongWritable &ndash; for numeric data</p>

<p>There are other data types as well.  But, let us stick with these data types for the sake of simplicity and problem scoping.</p>

<p>Our Mapper will have the following data types:</p>

<p><strong>Input</strong><br/>
<em>Key</em> : LongWritable<br/>
<em>Value</em> : Text</p>

<p><strong>Output</strong><br/>
<em>Key</em> : Text<br/>
<em>Value</em> : LongWritable</p>

<p>Ask yourself! Are these data types correct for Mapper?  If so, why?</p>

<p>We are going to communicate these data types to our Java program in the following way.</p>

<p>```java
public class MatchMakerMapper extends Mapper&lt;LongWritable, Text, Text, LongWritable>{</p>

<p>  @Override</p>

<pre><code>public void map(LongWritable key, Text value, Context output){


}
</code></pre>

<p>}</p>

<p><code>``
After the statement</code>extends Mapper`, you write the following things, in the following order:<br/>
1. Input Key Data Type<br/>
2. Input Value Data Type<br/>
3. Output Key Data Type<br/>
4. Output Value Data Type</p>

<p>Similarly, as first two arguments of the <code>map()</code> method, we write the input key data type and the input value data type.</p>

<p>Now is the time to write the Mapper logic.  The Core Java language acts on Strings and not on Text objects.  To convert Text object to String, we need to invoke <code>toString()</code> method.  In String processing, Java provides a method called <code>split()</code>.  This method is used to split a sentence into its separate words.  Generally speaking, this method is used to split a string using a specific delimiter.</p>

<p>Remember that our input value will be the content of one line of the input file.  For example, <code>jaja jojo</code>.  If this needs to be split into two separate words, we need to invoke <code>split(" ")</code> method on this.  Notice that the delimiting character specified is white-space.</p>

<p>Combining the above two paragraphs, we can write the following piece of code.</p>

<p><code>java
  String[] names = value.toString().split(" ");
</code></p>

<p>If you need to emit an output a key, value pair from mapper, you could just call the <code>output.write()</code> method with the output key and value.</p>

<p>In this mapper, we need to produce two outputs.  So, we need to call <code>output.write()</code> twice.  First time, key will be the first word, that is, <code>names[0]</code> and the second time, the key will be the second word, that is, <code>names[1]</code>.  Value will be 1 both the times.</p>

<p>There is a catch here. <code>names[0]</code> and <code>names[1]</code> are String objects.  But, output key data type is Text.  So, we need to create new Text objects from <code>names[0]</code> and <code>names[1]</code> like <code>new Text(names[0])</code> and <code>new Text(names[1])</code> respectively.</p>

<p>Thus, the output-emitting piece of code is here.</p>

<p><code>java
  output.write(new Text(names[0]),new LongWritable(1));
  output.write(new Text(names[1]),new LongWritable(1));
</code></p>

<p>Thus, assembling all the disjoint pieces, here is our complete story.</p>

<p>```java
package com.vivekGanesan;</p>

<p>import java.io.IOException;</p>

<p>import org.apache.hadoop.io.LongWritable;
import org.apache.hadoop.io.Text;
import org.apache.hadoop.mapreduce.Mapper;</p>

<p>public class MatchMakerMapper extends Mapper&lt;LongWritable, Text, Text, LongWritable> {</p>

<pre><code>@Override
public void map(LongWritable key, Text value, Context output) throws IOException, InterruptedException {

    //If more than one word is present, split using white space.
    String[] words = value.toString().split(" ");
    //each word contributes to a key-value pair
    output.write(new Text(words[0]), new LongWritable(1));
    output.write(new Text(words[1]), new LongWritable(1));
}
</code></pre>

<p>}
```</p>

<h3>Coding the Reducer</h3>

<p>Remember that the reducer&rsquo;s work starts only after &ldquo;Shuffle &amp; Sort&rdquo; has done its work.  That way, each reducer will be called with one group (key,{set of values}) as input at a time.</p>

<p>Similar to the case of Mapper, the Reducer also will have its own input and output data types.</p>

<p><strong>Input</strong><br/>
<em>Key</em> : Text<br/>
<em>Value</em> : LongWritable</p>

<p><strong>Output</strong><br/>
<em>Key</em> : Text<br/>
<em>Value</em> : LongWritable</p>

<p>Similar to the case of Mapper, any class which does the Reducing functionality should extend a <code>Reducer</code> class and should define a <code>reduce()</code> method.</p>

<p>So, our skeleton reducer goes like this:</p>

<p>```java</p>

<p>public class MatchMakerReducer extends Reducer {</p>

<p>  public void reduce(){</p>

<p>  }</p>

<p>}
```
Similar to the case of Mapper, same conventions apply here as to which data types go where.  Thus, we end up having the following code in Reducer.</p>

<p>```java
public class MatchMakerReducer extends Reducer&lt;Text, LongWritable, Text, LongWritable> {</p>

<pre><code>@Override
public void reduce(Text key, Iterable&lt;LongWritable&gt; values, Context output) {

}
</code></pre>

<p>}
```</p>

<p>Oops! I forgot to tell you what an <code>Iterable</code> is.  <code>Iterable</code> is anything which can be sequentially traversed.  Thus, our set of {1,1,1,&hellip;,1} falls under the category of <code>Iterable</code>.</p>

<p>In order to traverse an <code>Iterable</code>, we can use a <code>for</code> loop.  In each iteration, we need to increment a counter to count the number of times a person has appeared in the photos.</p>

<p>Thus, we get the following code.</p>

<p>```java</p>

<pre><code>    int count = 0;
    for(LongWritable value: values){
        count+= value.get();
    }
</code></pre>

<p>```</p>

<p>Then, we need a single <code>output.write()</code> method to collect the output key and value.  Output key is nothing but the input key, the name of the person.  Output value is the count.  Thus, we would write:</p>

<p>```java</p>

<pre><code>  output.write(key, new LongWritable(count));
</code></pre>

<p>```</p>

<p>Hereby, we present the full reducer as follows.</p>

<p>```java
package com.vivekGanesan;</p>

<p>import java.io.IOException;
import org.apache.hadoop.io.LongWritable;
import org.apache.hadoop.io.Text;
import org.apache.hadoop.mapreduce.Reducer;</p>

<p>public class MatchMakerReducer extends Reducer&lt;Text, LongWritable, Text, LongWritable> {</p>

<pre><code>@Override
public void reduce(Text key, Iterable&lt;LongWritable&gt; values, Context output)
        throws IOException, InterruptedException {
    int count = 0;
    for(LongWritable value: values){
        count+= value.get();
    }
    output.write(key, new LongWritable(count));
}
</code></pre>

<p>}
```</p>

<h3>Coding the Driver</h3>

<p>The work does not stop here.  We need to code a common templatized driver class to invoke the Mapper and Reducer.  Here is the templatized driver class called  <code>MatchMakerDriver</code>.</p>

<p>```java
package com.vivekGanesan;</p>

<p>import org.apache.hadoop.conf.Configuration;
import org.apache.hadoop.conf.Configured;
import org.apache.hadoop.fs.Path;
import org.apache.hadoop.io.IntWritable;
import org.apache.hadoop.io.Text;
import org.apache.hadoop.mapreduce.Job;
import org.apache.hadoop.mapreduce.lib.input.FileInputFormat;
import org.apache.hadoop.mapreduce.lib.input.TextInputFormat;
import org.apache.hadoop.mapreduce.lib.output.FileOutputFormat;
import org.apache.hadoop.mapreduce.lib.output.TextOutputFormat;
import org.apache.hadoop.util.Tool;
import org.apache.hadoop.util.ToolRunner;</p>

<p>public class MatchMakerDriver extends Configured implements Tool{</p>

<pre><code>public static void main(String[] args) throws Exception {
    int res = ToolRunner.run(new Configuration(), new MatchMakerDriver(), args);
    System.exit(res);       
}

@Override
public int run(String[] args) throws Exception {
    if (args.length != 2) {
        System.out.println("usage: [input] [output]");
        System.exit(-1);
    }

    Job job = Job.getInstance(new Configuration());
    job.setOutputKeyClass(Text.class);
    job.setOutputValueClass(IntWritable.class);

    job.setMapperClass(MatchMakerMapper.class);
    job.setReducerClass(MatchMakerReducer.class);

    job.setInputFormatClass(TextInputFormat.class);
    job.setOutputFormatClass(TextOutputFormat.class);

    FileInputFormat.setInputPaths(job, new Path(args[0]));
    FileOutputFormat.setOutputPath(job, new Path(args[1]));

    job.setJarByClass(MatchMakerDriver.class);

    job.submit();
    return 0;
}
</code></pre>

<p>}
```</p>

<p>Here ends our coding effort.</p>

<h3>Packaging the Code</h3>

<p>In order to run in Hadoop environment, the code has to be packaged in the form of a Java Archive (JAR) file.  If you would like to run your code in <a href="http://hortonworks.com/products/hortonworks-sandbox/">Hortonworks Sandbox</a> VM, make sure you compile your code using Java 6.</p>

<h3>Running &ndash; Finding the Husband</h3>

<p>The Husband can be found by running the JAR file in a Hadoop environment.  Refer to the <a href="https://github.com/hortonworks/hadoop-tutorials/blob/master/Community/T09_Write_And_Run_Your_Own_MapReduce_Java_Program_Poll_Result_Analysis.md">Hortonworks Community Tutorial</a> for instructions on how to run this in <a href="http://hortonworks.com/products/hortonworks-sandbox/">Hortonworks Sandbox</a> VM.</p>

<h3>Download the Code</h3>

<p>You can download all the code mentioned in this tutorial, along with dependencies from the github repository named <a href="https://github.com/vivganes/what-is-mapreduce-blog-post">vivganes/what-is-mapreduce-blog-post</a>.</p>

<p>I hope that this post was informative and useful.  Feel free to pen down your comments here or to email me at <a href="&#109;&#x61;&#x69;&#108;&#x74;&#111;&#x3a;&#x76;&#x69;&#118;&#101;&#107;&#x40;&#x76;&#105;&#x76;&#x65;&#107;&#103;&#x61;&#110;&#101;&#115;&#x61;&#110;&#x2e;&#x63;&#x6f;&#x6d;&#46;">&#x76;&#x69;&#x76;&#101;&#107;&#x40;&#x76;&#x69;&#118;&#101;&#x6b;&#103;&#97;&#110;&#x65;&#115;&#x61;&#110;&#46;&#x63;&#111;&#109;&#x2e;</a></p>
]]></content>
  </entry>
  
</feed>
